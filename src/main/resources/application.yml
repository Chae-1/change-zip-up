spring:
  profiles:
    active: prod
  config:
    import: "classpath:application-secrets-prod.yml"
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB


