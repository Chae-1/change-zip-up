spring:
  profiles:
    active: prod
  config:
    import: "classpath:application-secrets-prod.yml"
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 25MB


